# Quick Start

The fastest way to run JSON Schema Studio locally:

```bash
git clone https://github.com/ioflux-org/studio-json-schema.git
cd studio-json-schema
npm install
npm run dev
```

Open your browser at the URL shown in the terminal (`http://localhost:5173`).

For a more consistent environment, see the [Docker installation guide](/guide/installation).